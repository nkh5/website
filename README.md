# nkh5.github.io/website

Personal portfolio — silicon validation & design verification.
Static site: HTML + CSS + vanilla JS, no build step.

Serve locally: `python3 -m http.server` then open http://localhost:8000
